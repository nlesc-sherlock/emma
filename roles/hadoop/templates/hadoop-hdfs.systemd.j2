[Unit]
Description=hadoop-hdfs
After=syslog.target network.target

[Service]
User=root
Type=forking
Environment="JAVA_HOME={{ java_home }}"
ExecStart={{ hadoop_usr_dir }}/sbin/start-dfs.sh
ExecStop={{ hadoop_usr_dir }}/sbin/stop-dfs.sh

[Install]
WantedBy=multi-user.target
